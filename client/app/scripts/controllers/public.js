'use strict';

/**
 * @ngdoc function
 * @name clientApp.controller:PublicCtrl
 * @description
 * # PublicCtrl
 * Controller of the clientApp
 */
angular.module('clientApp')
  .controller('PublicCtrl', ['$transitions', '$scope', function ($transitions, $scope) {
  }]);
