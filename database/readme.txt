How To set up the SQLite database

1. Install SQLite with command: "sudo apt get sqlite3"
2. Navigate the terminal to the "database" folder
3. Enter "sqlite3" to open the database prompt
4. Enter ".read create_tables.sql" NOTE: this drops the tables every time it is run
5. Enter ".read sample_data.sql"